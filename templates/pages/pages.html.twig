{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div class="example-wrapper"><a href="{{ path('default') }}">на главную</a>\ <a href="{{ path('blog') }}">к страницам</a>
    <h1>{{ blogs.titlePage }}</h1>
    <p>{{ blogs.contentPage }}</p>
    <p>Дата публикации : {{ blogs.datePub| date('d/m/Y') }}</p>
    <p><a href="{{ path('edit_p',{'id':blogs.id }) }}">редактировать</a> </p>
    </div>
{% endblock %}

{% block title %}

{% endblock %}